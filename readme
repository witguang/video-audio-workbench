视频处理工作台（workbench_app.py）

当前主入口：workbench_app.py
核心处理模块：mode1_ffmpeg.py（调用 ffmpeg_utils.py）
辅助脚本：create_shortcut.py（在桌面创建启动快捷方式）

三种处理入口：
1. 本地电脑直处理：直接选择本机视频文件，导出音频，按需合成封面视频。
2. 本地文件夹批量：直接处理本机整个视频文件夹，可单独指定批量输出目录。
3. R2 URL 下载后处理：输入 R2 的视频链接，程序会先下载到本地临时文件，再复用同一套处理逻辑。

核心能力：
- 背景图为空时只导出音频；有背景图时额外生成封面视频。
- 支持同步歌词，可选开启「卡片歌词模式」，内含三种可切换视觉主题：
    · 极简高级 —— 封面居中为主角，暗渐变背景，粗体排版，金色点缀
    · 播放器分栏 —— 左侧大封面 + 右侧 Now Playing 排版，播放器风
    · 玻璃拟态 —— 整块磨砂玻璃面板 + 冰蓝高光，通透现代
  歌词支持 LRC / SRT / VTT / 纯歌词(plain/txt) 四种格式（按扩展名自动识别，
  SRT/VTT 用字幕自带时间轴，LRC 用下一句起点推断结尾）；渲染为 ASS 高亮
  （Spotify 配色）：当前行纯白高亮、上一行与下一行灰暗、淡入淡出。
- 「歌词显示」二选一（界面「卡片风格」行右侧下拉）：
    · 三行高亮 —— 默认，上一行/当前行/下一行三行静态排布，当前行纯白高亮。
    · 滚动歌词 —— 参考主流播放器卡拉OK 滚动：每行从底部匀速滚向顶部，经过
      中心高亮带时变亮、滚出后压暗，连续滚动观感；高亮以当前行活动窗口为准。
      滚动速度与滚入/滚出位置带可在 card_render.py 顶部 SCROLL_LAYOUT 调节。
- 纯歌词文件（如 .plain / .txt，只有歌词没有时间轴）会自动在相同目录查找同名 .srt，
  用「每行歌词 = 一个对齐单元」的方式把 SRT 时间轴逐行匹配到纯歌词上：token 级 LCS
  全局对齐（正确处理重复副歌）→ 只把配对置信度高的行作为锚点，弱匹配行按 token 占比
  在锚点间插值 → 起点做亚 cue 细分、严格单调、每行至少 2.2s 可读时长，避免歌词
  切换太快或两行抢同一条 cue，无需手动改 SRT。
- 反向也成立：选 .srt / .vtt 时，若同目录存在同主名 .plain / .txt，会自动改为
  「plain 文本 + SRT 时间轴」——无论选哪个文件，都以 plain 的一行一句歌词为准，
  SRT 只提供时间轴（SRT 常见错词/乱行切分，plain 才是正确歌词）。
- 「消除歌词时间重叠」开关（默认开启）：SRT/VTT 相邻两句时间间隔太短、时间轴互相
  重叠时，自动把每行结尾截断到下一行起点，避免两行歌词同时高亮叠在一起；需要保留
  原始字幕节奏时可取消勾选。
- 双语字幕（主英附中）：中文来源三选一——「关闭 / 中文翻译文件 / API 自动翻译」。
  任一开启后，当前行在英文下方附一行较小的中文译文，上一行与下一行仍只显示英文灰暗行；
  中文只提供译文文本，时间轴完全由英文行决定；双语模式下三行位置自动错开、字号收紧，
  避免两行高的当前行与上/下行重叠。
  · 中文翻译文件：plain/txt/lrc/srt/vtt，与英文歌词逐行 1:1 对应。
  · API 自动翻译：自动把英文歌词逐行翻译成中文（lyric_translate.py）。界面选接口并填
    API Key（仅存本机 app_settings.json，不随视频输出；也可设环境变量，界面输入优先）。
    支持以下官方 Chat Completions 接口（统一 OpenAI 兼容格式，接入新厂商只需在
    PROVIDERS 表补一条）：
      DeepSeek   https://api.deepseek.com/chat/completions                 deepseek-chat      DEEPSEEK_API_KEY
      通义千问   https://dashscope.aliyuncs.com/compatible-mode/v1/...     qwen-plus          DASHSCOPE_API_KEY
      Kimi       https://api.moonshot.cn/v1/chat/completions               moonshot-v1-8k     MOONSHOT_API_KEY
      智谱 GLM   https://open.bigmodel.cn/api/paas/v4/chat/completions     glm-4-flash        ZHIPU_API_KEY
      OpenAI     https://api.openai.com/v1/chat/completions                gpt-4o-mini        OPENAI_API_KEY
    翻译按行号逐行对应（带行号请求、漏译兜底），分批调用避免超长；翻译失败会中止处理并提示。
  · 翻译缓存（省 token）：同一首歌以「接口+模型+英文歌词全文」为键缓存中文结果到
    cache/lyric_zh/（不随视频输出、不入库），重复处理同一首歌不再调用 API；英文歌词
    或接口/模型变化会自动重新翻译。「清缓存」按钮可手动清除全部缓存。
- 处理范围统一为一项（导出音频 / 卡片视频 / 字幕共用）：只渲染所选时间段，音频先裁到范围、
  字幕时间轴整体平移对齐，再从相对 0 点渲染；保证音画严格同步，也避免先渲染整首歌再裁剪
  导致的卡死。留空起始或设 End 表示直到结尾。
- 批量模式下会自动规避同一轮任务中的重名输出。

卡片主题说明：
- 主题在界面底部「卡片风格」下拉框选择，选择结果会存入 app_settings.json。
- 「竖屏输出（9:16 适配手机）」勾选框：勾选后改为 1080x1920 竖屏画布输出，
  封面卡居中偏上、歌词在下方区域，适配抖音/快手/Reels/Shorts；默认不勾为 16:9 横屏。
  竖屏仅对卡片歌词模式有意义，普通静态视频模式不受影响。
- 渲染在 1920x1080（横屏）或 1080x1920（竖屏）内部画布完成，输出更清晰。
- 输出规格为轻量档：h264 1080p30（veryfast + CRF23）+ AAC 立体声（160k），兼顾速度与体积。
- 静态背景（模糊/暗角/遮罩/封面卡）只渲染一帧合成一次，编码阶段仅叠加 ASS 歌词，
  相比逐帧重算背景滤镜可提速约 10 倍。编码档位可在 card_render.py 顶部的
  VIDEO_FPS / VIDEO_PRESET / VIDEO_CRF / AUDIO_BITRATE 集中调整。
- 卡片模式依赖 Pillow（见 requirements.txt）。

FFmpeg 依赖：
- 程序运行时需要 ffmpeg（ffmpeg_utils.get_ffmpeg_path() 优先查找项目 ffmpeg/ffmpeg.exe，其次系统 PATH）。
- 因体积原因（约 180MB）ffmpeg.exe 不随仓库分发，请自行下载 Windows 版 ffmpeg
  （如 https://www.gyan.dev/ffmpeg/builds/ 的 release-full build），把 ffmpeg.exe 放入
  项目 ffmpeg/ 目录（与下方打包命令 ffmpeg/ffmpeg.exe 路径一致），或加入系统 PATH。

运行方式：
  python workbench_app.py

打包 EXE（示例）：
  pip install pyinstaller
  pyinstaller -F -w --add-binary "ffmpeg/ffmpeg.exe;ffmpeg" workbench_app.py

说明：
- 卡片歌词模式依赖 Pillow；普通静态视频模式不依赖（标准库 + 项目 ffmpeg/ 目录下的 ffmpeg.exe，见上）。
- create_shortcut.py 仅在需要修复 logo.ico 图标格式时用到 Pillow。
- 配置保存在 app_settings.json（个人本机路径，不入库）。
- 历史版本的 main_gui.py、update_project.py、mode1_ffmpeg copy.py 已归档至 _archive/。
